export interface SingleProjectResult {
    plugin: PluginMetadata;
    package: DepTree;
}
interface PluginMetadata {
    name: string;
    runtime: string;
    targetFile?: string;
}
interface JsonDepsScriptResult {
    defaultProject: string;
    projects: ProjectsDict;
}
interface ProjectsDict {
    [project: string]: DepDict;
}
interface DepDict {
    [name: string]: DepTree;
}
interface DepTree {
    name: string;
    version: string;
    dependencies?: DepDict;
    packageFormatVersion?: string;
}
export declare function inspect(root: any, targetFile: any, options?: any): Promise<SingleProjectResult>;
declare function extractJsonFromScriptOutput(stdoutText: string): JsonDepsScriptResult;
declare function buildArgs(root: any, targetFile: any, gradleArgs: string[]): string[];
export declare const exportsForTests: {
    buildArgs: typeof buildArgs;
    extractJsonFromScriptOutput: typeof extractJsonFromScriptOutput;
};
export {};
